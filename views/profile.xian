{{> head}}
<body class="bg-gradient-to-br from-blue-50 to-cyan-50 min-h-screen">
  {{> navbar}}

  <div class="max-w-4xl mx-auto px-4 py-8" style="padding-top: 100px;">
    <a href="/dashboard" class="inline-flex items-center text-blue-600 hover:text-blue-800 mb-6 transition-all duration-300 font-bold uppercase tracking-wide hover:gap-3 gap-2">
      <span class="text-xl">←</span> Back to Dashboard
    </a>
    <div class="bg-gradient-to-br from-white to-blue-50 rounded-3xl shadow-2xl p-10 border-2 border-blue-200 hover:shadow-blue-500/20 transition-all duration-300">
      <div class="flex items-center justify-center gap-4 mb-8">
        <span class="text-6xl">👤</span>
        <h2 class="text-3xl sm:text-4xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-blue-600 via-cyan-600 to-blue-700 uppercase tracking-tight">My Profile</h2>
      </div>

      {{#if success_msg}}
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-6">
          {{success_msg}}
        </div>
      {{/if}}

      {{#if error_msg}}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-6">
          {{error_msg}}
        </div>
      {{/if}}

      <form method="POST" action="/profile">
        <div class="mb-8">
          <label class="block text-gray-800 text-sm font-extrabold mb-3 uppercase tracking-wide flex items-center gap-2">
            <span class="text-xl">👤</span> Full Name
          </label>
          <input type="text" name="name" value="{{user.name}}" required 
            class="w-full px-5 py-4 border-2 border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-300 font-medium hover:border-blue-400" />
        </div>

        <div class="mb-8">
          <label class="block text-gray-800 text-sm font-extrabold mb-3 uppercase tracking-wide flex items-center gap-2">
            <span class="text-xl">📧</span> Email Address
          </label>
          <input type="email" name="email" value="{{user.email}}" required 
            class="w-full px-5 py-4 border-2 border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-300 font-medium hover:border-blue-400" />
        </div>

        <div class="mb-8">
          <label class="block text-gray-800 text-sm font-extrabold mb-3 uppercase tracking-wide flex items-center gap-2">
            <span class="text-xl">📱</span> Phone Number
          </label>
          <input type="tel" name="phone" value="{{user.phone}}" 
            class="w-full px-5 py-4 border-2 border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-300 font-medium hover:border-blue-400
            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500">{{user.address}}</textarea>
        </div>

        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
          <p class="text-sm text-blue-800"><strong>Account Role:</strong> {{userRole}}</p>
          <p class="text-sm text-blue-800"><strong>Member Since:</strong> {{user.createdAt}}</p>
        </div>

        <button type="submit" 
          class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 rounded-lg transition">
          Update Profile
        </button>
      </form>
    </div>
  </div>
{{> footer}}
